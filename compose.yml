
services:

  rvms-vitepress:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        APP_ENV: ${APP_ENV}
        IMAGE_PREFIX: ${IMAGE_PREFIX}
        IMAGE_EXT_PREFIX: ${IMAGE_EXT_PREFIX}
    ports:
      - "${RVMS_VITEPRESS_PORT:-8080}:80"
